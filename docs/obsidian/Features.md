---
tags: [features]
---

# Features

Related: [[Business-Rules]] · [[APIs]] · [[Frontend]] · [[Changelog]]

This is an inventory of features **confirmed implemented** (live route + live UI, or live route with a documented consumer), built from the same route-by-route and frontend-structure reads behind [[APIs]] and [[Frontend]]. Where a route exists but no frontend consumer was confirmed, or vice versa, that's noted rather than assumed.

## Owner-facing (`apps/frontend` owner routes → `app/components/views/*`)

| Feature | Route | Backing |
|---|---|---|
| Portfolio dashboard | `/dashboard` | `PortfolioView` → `/api/dashboard*`, `/api/owner/portfolio/summary` |
| Hostel detail / config | `/hostels/:hostelId(/:tab)` | `HostelDetailView` → `/api/hostels/[id]/*` (billing/automation/notification/payment/receipt/security/system config) |
| Tenant portfolio & profile | `/tenants`, `/hostels/:hostelId/tenants/:tenantId` | `TenantsPortfolioView`, `TenantProfileRoute` → `/api/tenants/*` |
| Bulk tenant import | `/tenants/import` | `BulkInvitationImportView` → `/api/bulk-import/*` |
| Move-outs | `/move-outs` | `MoveOutsView` → `/api/move-out/requests/*` — full inspection/dispute/settlement workflow |
| Agreement renewals | `/agreements/renewals` | `RenewalQueueView` → `/api/agreements/renewal-offers*`, `/api/agreements/renewals` |
| Agreement lifecycle recovery | `/agreements/lifecycle-recovery` | `AgreementLifecycleRecoveryView` → `/api/agreements/lifecycle-recovery*` |
| Alerts | `/alerts` | `AlertsView` |
| Billing / financial control center | `/billing` | `BillingView` + `FinancialControlCenter.tsx` (Overview tab: Collection Progress, Priority Collections, Smart Insights, Property Finance (all-hostels view only), Recent Transactions with expandable full `PaymentLedger`) + `ExpensesTab` (Expenses Workspace tab) — redesigned 2026-07-18, see [[Changelog]]; most of the older `billing/*` widget set (cashflow/collection-pipeline/overdue-intelligence/room-performance charts) was dead code by this point and was removed |
| Settings | `/settings` | `SettingsView` |
| Activity log | `/activity` | `ActivityLogsView` → `/api/owner/activity-logs` |
| Admissions/CRM | (routed but currently redirects to `/dashboard` per `OwnerRoutes.tsx` — **flag: route exists in backend (`/api/leads/*`) but the dedicated `/admissions` frontend route redirects rather than rendering `AdmissionsView` directly; confirm current wiring before documenting as fully live**) | `AdmissionsView` component exists; `/api/leads/*`, `/api/visit/*` (public microsite) |
| Owner WhatsApp assistant | (no dedicated frontend route found — WhatsApp-native) | `/api/owner/whatsapp/*`, `lib/services/notifications/owner-whatsapp-assistant.ts` (7180 lines) — conversational, ID-based interactive menus |
| Owner daily briefings | (WhatsApp/email-delivered, no frontend page) | `/api/cron/daily-briefings`, `briefing-engine.ts` |
| Expenses | `/billing?tab=expenses` (`Expenses Workspace` tab, sibling to `Overview`; not a standalone route) | `ExpensesTab` orchestrator + `hostel-detail/tabs/expenses/*` component set (dashboard, filters, list with row selection, Add/Edit modal with searchable category picker, Details modal with receipt preview/decision-support, CSV/Excel/PDF export via `ExpenseExportMenu`) → `/api/expenses*`, `/api/expenses/export`. Portfolio-level only — always queries `hostelId=all`; every expense created here is forced to `expense_scope: BUSINESS` (`hostel_id: null`) by the API wrapper, regardless of the page's hostel selector. Export reports (PDF business report + XLSX with a Metadata/Financial Summary/Category Breakdown header block) carry a dynamic title, full metadata (generated by/at, reporting period + duration, filter snapshot, report version), and financials computed via the same shared functions the dashboard's KPI cards use — see [[Decisions]] ADR-010, [[Business-Rules]]. |
| Change-request approval workflow (owner side: propose/cancel) | Surfaced within tenant profile | `features/change-management` (most fully-built feature module) → `/api/change-requests/*` |
| Owner Action Registry (catalog only) | Surfaced within `TenantProfilePage.tsx` (`PrimaryActionsBar` "Request Change" / "Receive Payment" labels) | `ownerActionRegistry` (`apps/backend/src/services/owner-actions/`) → `GET /api/owner-actions?entity=tenant&tenantId=<id>` → `features/owner-actions/api` (`ownerActionsService.listForTenant`) — see below |
| Correct Payment (Reverse / Transfer) | Surfaced within `TenantProfilePage.tsx` → Activity tab's unified timeline (per-payment "Correct Payment" button) | `CorrectPaymentModal` (`apps/frontend/src/app/components/modals/`) → `features/recovery/api` (`recoveryService`), `features/payments/api` (`paymentService.quickCollectSearch` for the Transfer tenant picker) → `/api/recovery/cases*` (Business Recovery Platform) — see below |
| Tenant Profile UI/UX consolidation | `apps/frontend/src/features/tenants/components/profile/TenantProfilePage.tsx` | No new backend routes — pure frontend recomposition of the existing page; see below |

### Owner Action Registry
- **Status:** shipped (catalog-only slice)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `apps/backend/src/services/owner-actions/owner-action-registry.ts` (in-memory registry, `listForEntity(entity, ctx)`), `apps/backend/src/services/owner-actions/definitions/*.ts` (`tenant-actions.ts` → `TENANT_EDIT_PERSONAL_INFO`; `payment-actions.ts` → `PAYMENT_RECEIVE`; `room-actions.ts` → `ROOM_MOVE`), `apps/backend/src/services/owner-actions/bootstrap.ts` (single import point for all definitions), `apps/backend/app/api/owner-actions/route.ts`, `apps/frontend/src/features/owner-actions/api/index.ts`
- **Depends on:** [[APIs]] (`GET /api/owner-actions`)
- **Notes:** A read-only catalog of which owner-facing actions are available for a given entity (currently only `tenant` is queried by the frontend). Each registered action declares `entity`, `category` (`EDIT`/`WORKFLOW`/`CORRECTION`/`VIEW`), a `label`, `allowedRoles`, and a synchronous `isAvailable(ctx)` predicate (evaluated server-side against `tenantStatus`/`actorRole`). `TenantProfilePage.tsx` fetches the tenant's action list and uses it to source the labels on the "Request Change" (`TENANT_EDIT_PERSONAL_INFO`, available only when tenant status is `ACTIVE`) and "Receive Payment" (`PAYMENT_RECEIVE`, always available) buttons on `PrimaryActionsBar`, falling back to hardcoded defaults if the catalog lookup misses. **This is catalog-only in this first slice — no action's actual execution routes through the registry yet** (clicking the buttons still triggers the pre-existing change-request/payment flows directly); the registry exists so future `CORRECTION`-category actions (payment reversal/transfer, expense void, etc. — see the Business Recovery Platform / correction-cases proposal work under `docs/business-logic/`, not yet reflected elsewhere in this vault) have a place to register into without inventing a new mechanism. `ROOM_MOVE` (`entity: "room"`) is registered but has no frontend consumer yet — deliberately out of scope for this slice.

### Correct Payment (Reverse / Transfer)
- **Status:** shipped (Reverse + Transfer — first real UI consumer of the Business Recovery Platform's `/api/recovery/cases/*` routes)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `apps/frontend/src/features/recovery/api/index.ts` (`recoveryService.createCase/validate/execute/getById`), `apps/frontend/src/app/components/modals/CorrectPaymentModal.tsx` (self-contained correction-type toggle → reason (+ tenant picker for Transfer) → preview → confirm modal), `apps/frontend/src/features/payments/api/index.js` (`paymentService.quickCollectSearch`, reused for the Transfer "correct tenant" search, same pattern as `RecordPaymentModal.tsx`), `apps/frontend/src/features/tenants/components/financial/FinancialActivityCard.tsx` (`onCorrectPayment` prop, "Correct Payment" button next to "View Receipt"), `UnifiedActivityTimeline.tsx` (threads the prop), `apps/frontend/src/features/tenants/components/profile/TenantProfilePage.tsx` (`correctingPaymentId` state, renders the modal, invalidates `queryKeys.tenants.{obligations,financialTimeline,advance,full,overview}` on close)
- **Depends on:** [[APIs]] (Recovery / Corrections), [[Business-Rules]] (Correction Cases — Payment corrections)
- **Notes:** Wired directly into the Tenant Details page's Activity tab's unified timeline per an explicit product decision to **not** build a separate Recovery Center dashboard in this phase (that list/detail view is deferred to Phase 3). This bypasses the Owner Action Registry entirely — it does not register as a `CORRECTION`-category owner action; the "Correct Payment" button is a conditional render (`receiptPaymentId && onCorrectPayment && canCorrectPayment`) alongside the existing "View Receipt" button, not sourced from `ownerActionsService`. The third condition (`canCorrectPayment = payments.length <= 1`) hides the button for grouped/multi-payment settlement entries — see [[Bugs]] — because reversing/transferring one payment in a group would only be a partial correction of the card's total. The modal now opens with a two-button correction-type toggle ("Wrong / Reverse" vs "Wrong Tenant / Transfer", pre-preview only — hidden once a case exists). Reverse mode is unchanged: `createCase({caseType: 'PAYMENT_REVERSAL', input: {paymentId}})`. Transfer mode additionally requires picking a destination tenant via a debounced (250ms) `paymentService.quickCollectSearch` lookup (fires only when the query is ≥2 chars and no tenant is yet selected) before "Preview Impact" is enabled, then calls `createCase({caseType: 'PAYMENT_TRANSFER', input: {paymentId, toTenantId}})`. Both modes share the same preview → "Confirm Reversal"/"Confirm Transfer" → `validate` + `execute` flow (treated as one logical confirm step; a 422 `VALIDATION_REJECTED` from `validate` stops `execute` from running and shows the rejection reason inline). Edit Reference/Notes (`PAYMENT_REFERENCE_EDIT`) UI is still explicitly out of scope for this slice — the backend handler exists and is reviewed, but has no frontend entry point yet. Reverse mode also supports a guided immediate re-record for the "wrong amount" case: `CorrectPaymentModal` accepts an optional `onSuccessReverse?: (tenantId: string) => void` callback, invoked only when `correctionType === 'REVERSE'` (never Transfer, since a Transfer's money has already moved to the correct tenant, leaving nothing to re-record). `TenantProfilePage.tsx` wires this to reopen the existing Receive Payment flow (`RecordPaymentModal`, reusing the page's existing payment-modal state — no second mount point was introduced) about 1.3s after the reversal's own success state finishes displaying, scoped to the same tenant whose payment was just reversed.

### Change Rent
- **Status:** shipped
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `apps/frontend/src/features/tenants/api/index.js` (`tenantService.changeRent`), `apps/frontend/src/app/components/modals/ChangeRentModal.tsx` (self-contained FORM → CONFIRM flow, mirroring `WaiveObligationModal.tsx`'s identity-confirmation pattern), `apps/frontend/src/features/tenants/components/profile/TenantProfilePage.tsx` (`showChangeRent` state, "Change Rent" surfaced as one of `PrimaryActionsBar`'s own overflow ("More") menu actions — not a separate adjacent button — gated on `canChangeRent` (tenant status `ACTIVE`), `upcomingRentObligations` derived from the page's already-loaded `dues.items` — no second obligations fetch)
- **Depends on:** [[APIs]] (`POST /api/tenants/:id/change-rent`), [[Business-Rules]] (rent changes are immediate and owner-only, see the Category C exception note)
- **Notes:** Owner picks a new rent amount, the month it should take effect from (dropdown sourced from `upcomingRentObligations` — RENT obligations from the page's existing dues data with zero recorded *net* payments, `Number(o.paid ?? 0) === 0`), and a reason → password re-entry (`identityService.confirmIdentity(password, 'CHANGE_RENT')`) → submit. Applies immediately with no tenant approval step (identity-token gated the same as obligation cancel/waive). On success, invalidates `queryKeys.tenants.{obligations,financialTimeline,advance,full,overview}` and calls the page's `refetch()`. `currentRent` is read from `tenant?.monthly_rent ?? overview?.rent`. `applyRentChangeInTx` now also writes `tenants.monthly_rent` (in the same transaction as `agreement.contract_rent`, mirroring the `tenantContractSync` pattern used by renewal activation in `renewal-activation-engine.ts`) — resolves the previously-flagged "Unknown / needs clarification": `tenant.monthly_rent` **is** kept in sync, so this displayed value no longer goes stale after a successful change. See [[Bugs]] for this fix and a second, related gap: the modal's pre-submit "N installments will change" preview is a client-side count filtered by *net* zero-paid, which is a looser guard than the backend's real safety rule (zero payment *records* on the obligation, `payments.length === 0`) — after a Payment Reversal correction (see Correct Payment above), an obligation can have net paid `0` via two payment rows (original + offsetting reversal) and still be correctly skipped by the backend while still appearing in the frontend's preview count. The modal does not attempt to prevent this (would require exposing raw payment-row counts through `getTenantDues()`, out of scope); instead its success screen now shows the server-reported `obligationsUpdated` count from the response, so any such discrepancy is visible after the fact instead of silent.

### Tenant Profile UI/UX consolidation
- **Status:** shipped
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `apps/frontend/src/features/tenants/components/profile/TenantProfilePage.tsx` (orchestrator — now ~840 lines, down from having 6 overlapping sections), `RiskComplianceCard.tsx` (replaces `TenantHealthCard.tsx` + `OwnerInsights.tsx`), `UnifiedActivityTimeline.tsx` (replaces `FinancialActivity.tsx` + `LedgerStatement.tsx` + `ActivityTimeline.tsx`, and absorbs invitation-history logs that previously had their own standalone card), `DocumentsTab.tsx` (replaces the separate KYC verification card + the old `DocumentsHub` section), `apps/frontend/src/features/tenants/components/financial/PrimaryActionsBar.tsx` (extended — primary "Receive Payment" button + 3 secondary buttons — Create Charge, Create Rent, View Receipts — + a "More" overflow menu — Share Payment Link, Request Change/Edit Details, Change Rent (conditional on `canChangeRent`), Check-out/Exit), `apps/frontend/src/app/components/ui/tabs.tsx` (real `Tabs`/`TabsList`/`TabsTrigger`/`TabsContent` primitive, replacing the deleted `FinancialWorkspaceNav.tsx` fake scroll-nav)
- **Depends on:** [[Frontend]] (page structure), existing services only — `financial-read-model-service.ts`, `tenantService`, `paymentService`, `ownerService`, `useTenantProfile`, `useOwnerActions`, `useTenantChangeRequests` are all reused unchanged
- **Notes:** Four-part consolidation of a page that had accumulated redundant sections over several prior feature slices (Owner Financial Workspace, Change Rent, Correct Payment). (1) `RiskComplianceCard` merges what used to be two independently-derived agreement/KYC/score cards (`TenantHealthCard`'s composite score + checklist, `OwnerInsights`'s prose warnings restating the same checklist items) into one checklist-first card. (2) The page's detail region is now real `Tabs` (`TabId = 'obligations' | 'activity' | 'documents' | 'stay'`, `activeTab` state) instead of the deleted `FinancialWorkspaceNav`'s scroll-into-view fake navigation. (3) `UnifiedActivityTimeline` (the Activity tab) merges what used to be three-to-four separate feeds — "Financial Activity" (payments/obligations events), "Ledger & Accounting Statement" (`advance` ledger entries), a standalone "Recent Activity" card, and a standalone "Invitation History Logs" card — into one filterable timeline, still surfacing the "Correct Payment" button per payment entry (see Correct Payment (Reverse / Transfer) above) via the same `onCorrectPayment` prop threaded through `FinancialActivityCard.tsx`. (4) `DocumentsTab` merges a standalone KYC verification card with the pre-existing `DocumentsHub` into one tab. Separately, the page's action layout (previously 3 groups totaling 8 buttons, including a standalone "Change Rent" button rendered next to the action bar) is now one `PrimaryActionsBar`: 1 primary + 3 secondary always-visible buttons, plus a "More" dropdown/bottom-sheet holding the remaining 4 actions (Share Payment Link, Request Change, Change Rent, Check-out/Exit) — `canChangeRent`/`onChangeRent` props gate the Change Rent item to `ACTIVE` tenants only, same rule as before. A 4th "Stay" tab (room allocation history via `AllocationHistoryTimeline` + the move-out settlement workflow via `ExitWorkflowSection`) was added, replacing what was previously a collapsible card lower on the page. **Purely a frontend recomposition — no backend routes, services, or business rules changed.** See [[Frontend]] for the resulting always-visible-zone + 4-tab structure, and [[Changelog]] for the dated entry.

### Flexible Payment Links (amount-flexible, tenant-scoped + tenant self-service generation)
- **Status:** shipped
- **Owner-facing?** yes (pre-existing, `PrimaryActionsBar`'s "Share Payment Link" — see Tenant Profile UI/UX consolidation above) · **Tenant-facing?** yes (link generation from `/tenant/financials`, plus the payer-facing `/pay/[token]` page itself, which needs no login)
- **Key files:** `apps/backend/src/services/payments/payment-link-service.ts` (`PaymentLinkService.getOrCreateToken` — `obligation_id` is now an optional default-amount hint, no longer resolved-and-required; unconditionally mints a tenant-scoped token even with zero outstanding obligations), `apps/backend/app/api/payments/pay-link/route.ts` (accepts `TENANT`-role sessions in addition to `OWNER`; a tenant session is force-scoped to its own `session.tenant_id` and forbidden from passing `obligationId` — only an owner may target an arbitrary tenant/obligation), `apps/backend/app/api/payments/pay/[token]/route.ts` (payer page now renders an editable amount input instead of a fixed obligation balance, with a live `preview` action showing the FIFO settlement breakdown as the payer types, and a default/checkout branch that calls the new `paymentService.createAmountPaymentIntent`), `apps/backend/src/services/payments/payment-service.ts` (`createAmountPaymentIntent` — amount-first counterpart to `createMultiObligationPaymentIntent`, builds its obligation set from `PAYABLE_STATUSES` and feeds it through `buildSettlementPlan`), `apps/frontend/src/portal/pages/TenantFinancialsPage.tsx` (`handleSharePaymentLink` handler + a "Share Payment Link" button rendered next to "Make Payment" in the Financial Health hero card), `apps/frontend/src/features/payments/api/index.js` (`paymentService.generatePayLink({ tenantId, obligationId })`, pre-existing wrapper, reused unchanged).
- **Depends on:** [[APIs]] (`POST /api/payments/pay-link`, `/api/payments/pay/[token]`), [[Business-Rules#Flexible payment links]], [[Database]] (`payment_link_tokens`), [[Decisions]] ADR-017, `src/services/payments/settlement-planner.ts::buildSettlementPlan` (the same FIFO engine offline "Receive Payment" uses).
- **Notes:** Multi-task branch. Earlier tasks made `payment_link_tokens.obligation_id` nullable and `getOrCreateToken` obligation-optional (so "Share Payment Link" now shows regardless of outstanding dues), then opened `POST /api/payments/pay-link` to `TENANT` sessions (self-generation, scoped strictly to their own account, tested in `apps/backend/tests/pay-link-tenant-auth.test.ts`), then reworked the payer-facing `/pay/[token]` page to accept any entered amount with a live FIFO-allocation preview rather than a fixed pre-picked obligation balance. The final task added the tenant-portal UI entry point so a tenant can copy a `/pay/<token>` link (from the response's `url` field) to send to a guardian or anyone paying on their behalf, without needing the owner to generate it for them. `TenantFinancialsPage.tsx` is technically under the frozen `src/portal/` tree (see [[Frontend]]) — this was an in-place edit to an already-allowlisted file, not a new file, so `check-architecture.mjs`'s allowlist was not touched.

## Tenant-facing (`apps/frontend` tenant routes, routed via `platforms/tenant/router` — rebuilt 2026-07-26 around `Stayo Tenant.dc.html`'s flat 5-tab IA; superseded `/tenant/{dashboard,financials,payments}` no longer exist)

| Feature | Route | Backing |
|---|---|---|
| Home | `/tenant/home` | `TenantHomePage` (`platforms/tenant`) → composes financial-read-model, food schedule/voting, announcements, events, open service-request status |
| Money | `/tenant/money` | `TenantMoneyPage` (`platforms/tenant`) → `useTenantFinancials()`, salvaged from the frozen tree's `TenantFinancialsPage.tsx` (financial-read-model, billing-timeline, billing-frequency, payment history, PhonePe checkout-redirect intent, pay-link generation) |
| Room | `/tenant/room` | `TenantRoomPage` (`platforms/tenant`) → `useTenantRoom()`, `/api/tenants/me/room` (room/roommates/utility-status/house-rules) + `/api/tenants/me/service-requests` (6-type generic request form) |
| Food | `/tenant/food` | `TenantFoodPage` → food schedule + voting (see Food feature entry) |
| Profile | `/tenant/profile` | `TenantProfilePage` (`platforms/tenant`) → static nav rows to the 4 sub-pages below, plus sign-out |
| Personal details | `/tenant/profile/details` | `TenantProfilePortalPage` (frozen `src/portal` tree, salvaged unrestyled) → `/api/tenants/me/profile`, change-request flow for governed fields |
| Agreement & renewal | `/tenant/renewal` | `TenantRenewalPage` (`platforms/tenant`, built 2026-07-22, unrouted until the 2026-07-26 rebuild) |
| Move-out | `/tenant/move-out` | `TenantMoveOutPage` (frozen `src/portal` tree) → `/api/move-out/tenant`, `/api/move-out/timeline` |
| Help & support | `/tenant/profile/help` | `TenantHelpPage` (`platforms/tenant`) |
| Payment return handling | `/payment-return` | `TenantPaymentReturnPage` (frozen `src/portal` tree) — the PhonePe checkout-redirect landing page; wired 2026-07-27 (was a `RouteScaffold` stub, see [[Bugs]]) |
| Activation / onboarding | `/activate(/:token)`, `/complete-profile` | `ActivateAccountPage`, `CompleteProfilePage` (frozen `src/portal` tree) → `/api/tenants/activate*`, `/api/tenants/me/complete-profile` |
| Billing-frequency change requests | (within Money/Profile) | `/api/tenants/me/billing-frequency` |
| Agreement renewal decisions | (within tenant flows) | `/api/tenant/renewal-offer/*` |

## Public-facing (`apps/frontend` public routes)

| Feature | Route | Backing |
|---|---|---|
| StayO marketing landing page | `/` | `app/pages/public/LandingPage.tsx` — see "StayO owner acquisition journey" below. Replaced the legacy `HomePage.tsx` (now `@deprecated`, orphaned) as of the StayO rebuild's owner-journey phase. |
| Legacy marketing site (remaining pages) | `/about`, `/facilities`, `/rooms`, `/gallery`, `/location`, `/contact`, `/pricing` | `app/pages/public/*`, Sanity CMS (`lib/sanity/`) — not yet migrated to the StayO design |
| Legal pages | `/legal/*` | `content/legal.ts` |
| Hostel admissions microsite | `/visit/:hostelSlug` | `/api/visit/[hostelSlug]*` — public lead capture, honeypot-protected |
| Receipt verification | `/verify/r/:token` | `/api/verify/receipt` — public, signed-token |

## StayO rebuild (new, in progress)

The StayO redesign is being built in place inside the same `apps/frontend` tree, per the design source in `Stayo Homepage Design (5)/` and `Stayo SaaS redesign/` (see `docs/migration/` for the full migration plan/tracker). Entries below are StayO-specific; the tables above still describe the legacy Shri Adithya feature set that StayO is progressively replacing.

### StayO owner acquisition journey — lead capture (Landing → mock Google sign-in → Hostel Details/OTP popup → "we'll be in touch")
- **Status:** superseded for the primary CTA path (2026-07-25) — see "StayO owner app — real signup/onboarding backend integration" below. `HostelLeadModal`/`LeadSubmittedPage`/`ActivationLinkPage` still exist and still work if directly navigated to, but `LandingPage.tsx`'s owner CTAs no longer route through them. **Further superseded 2026-07-28/29** — see "StayO owner-acquisition funnel — real lead capture → admin approval → activation" below, which made the "mock Google sign-in"/"OTP popup" this entry describes fully real.
- **Owner-facing?** yes · **Tenant-facing?** no (the shared `LoginModal` is still used for the nav's tenant login/signup, mode-aware per the design, but the owner path no longer uses it — see below)
- **Key files:** `app/pages/public/LandingPage.tsx` + `components/HeroShowcase.tsx`; `shared/ui-patterns/{LoginModal,GoogleSignInModal}.tsx`; `features/owner-onboarding/components/HostelLeadModal.tsx`; `features/owner-onboarding/context/MockOwnerJourneyContext.tsx`
- **Original click path (now orphaned, still functional if linked to directly):** `GoogleSignInModal` (mock-only "Continue with Google") → `HostelLeadModal`, a 3-step popup (Hostel Owner Name/Hostel Name/Phone Number → 6-digit phone OTP, mock-verified → "we'll be in touch" confirmation) → `MockOwnerJourneyContext.updateLead()`, dead-ending with no auto-continue into Onboarding.
- **As of 2026-07-25**, `openOwnerAuth()` still opens the same `GoogleSignInModal` first, but on success navigates straight to `/onboarding` (the real wizard) instead of opening `HostelLeadModal` — see the new entry below for the real signup flow this leads into. `LeadSubmittedPage`/`ActivationLinkPage` (`/get-started/submitted`, `/get-started/activate/:token`) remain reachable only by direct navigation, still gated by `RequireMockOwnerJourney`/`MockOwnerJourneyContext`, kept as-is and out of scope for the real-journey work. Tenant login (top nav) is unchanged and still uses `LoginModal`.
- **Depends on:** [[Frontend]] (StayO foundation: `ThemeProvider`, `OwnerAppShell`, `shared/ui-patterns/*`), `docs/migration/frontend-foundation-tracker.md` (routing-approach note)
- **Notes:** End-to-end click-through of the owner side of `Stayo Homepage.dc.html` → `AuthModal.dc.html` → `Owner Onboarding.dc.html` (12 steps, full animated SVG scene) → `Stayo App.dc.html`'s Home tab, faithfully ported. Two screens (Lead Submitted, Activation Link) don't exist in the design source and are now orphaned (see above). The dashboard screen this journey ends on (`OwnerDashboardPreviewPage`, at `/get-started/home`) renders `OwnerHomeDashboard`, a pure presentational component — still mock-data-driven as of this entry (real Dashboard wiring is the next phase after the signup/onboarding work below).

### Food — daily operations and the end of the mock Polls tab
- **Status:** shipped 2026-08-05 — **Phases 0 and 1 of 3** · see [[Decisions#ADR-048|ADR-048]] · **full module page: [[Food]]**
- **Owner-facing?** yes · **Tenant-facing?** copy only (the tenant Food tab's behaviour is unchanged)
- **Routes:** `/owner/food` (Today card now leads the tab) · `/owner/food/kitchen` (new)
- **Key files:** Backend — `lib/services/food/{schedule-rebuild-policy,voting-expiry}.ts` (new), `app/api/food/schedules/generate/route.ts`, `app/api/cron/food-carry-forward/route.ts`, `vercel.json`, `tests/food-{schedule-generator,schedule-rebuild-policy,voting-expiry}.test.ts` (new, 22 pure tests). Frontend — `features/owner-food/{weekGrid,publishChecks,kitchenSheet}.ts` + their tests (new, 41 tests), `components/today/TodayCard.tsx`, `components/HostelSwitcher.tsx`, `components/schedule/PublishChecklist.tsx`, `pages/KitchenSheetPage.tsx` (all new), `pages/FoodPage.tsx`, `hooks/useFoodSchedule.ts`, `features/owner-dashboard/components/OwnerHomeDashboard.tsx`, `shared/ui-patterns/Toast.tsx`.
- **Phase 0 — safety.** A `PUBLISHED` schedule can no longer be demoted to `DRAFT` or wholesale deleted: `POST /api/food/schedules/generate` gained `mode: BUILD | FILL_GAPS | START_OVER`, answers `409 SCHEDULE_PUBLISHED` when refused, and allows only `FILL_GAPS` against a published month. The `food-carry-forward` cron is registered for the first time (`0 1 * * *`) and its daily loop now also auto-closes voting periods past `voting_ends_at`. **The mock Food Polls tab is deleted** (~842 lines net) and the Home Quick Action repointed at the real thing — reversing [[Decisions#ADR-029|ADR-029]] point (3), per explicit user direction.
- **Phase 1 — the daily workflow.** Today card on the Food tab and a current-meal line on owner Home; inline add-item in the meal picker; publish pre-flight checks that **inform and never block**; an undo toast on live edits that names the weekday blast radius; a hostel picker closing an `hostels[0]` violation; and a Kitchen sheet (print + `wa.me` share, no backend, no Meta template) for the cook and kitchen staff, who had no surface at all.
- **The contract that matters:** every week-reader consumes one `WeekGrid` shape, never raw `food_schedules` rows — the seam that lets saved templates be added later without touching any consumer. Design doc §8.1, binding.
- **Notes:** food went from **zero tests to 63**. The weekly-pattern grain is unchanged and remains the module's ceiling — no `serve_date`, so festival/exam menus stay unrepresentable and per-date history was never recorded. **Phases 2–3 are not built**; [[Food]] §13 lists exactly what.

### Action Center work queues — one interaction model across all four cards
- **Status:** shipped 2026-08-05 — **Phase 3 of 4** · see [[Decisions#ADR-046|ADR-046]]
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `features/owner-workqueue/{WorkQueue.tsx,AgreementQueuePage.tsx,VacancyQueuePage.tsx}` (new), `features/owner-tenants/pages/PendingActivationsPage.tsx` (rebuilt on the shared component), `features/owner-collection/CollectionQueuePage.tsx` (refactored onto it), `shared/ui-patterns/StatCard.tsx` (gained `onClick`), `platforms/owner/router/OwnerRoutes.tsx`, `features/owner-dashboard/components/OwnerHomeDashboard.tsx`.
- **Routes:** `/owner/money/collect` · `/owner/agreements/review` · `/owner/tenants/activations` · `/owner/rooms/vacant`.
- **The model:** card → prioritised queue → one-tap actions → the row leaves the list. Every card obeys it because there is **one** `WorkQueue` implementation; a queue supplies data and actions and cannot invent layout, ordering or empty states.
- **No new backend.** Agreements reuse `getOwnerRenewalQueue` (already ranked and counted server-side), activations reuse the activation state machine via `usePendingActivations`, vacancy composes the existing `GET /api/rooms` across hostels.
- **Notes:** No progress bar or daily target by explicit product direction — completion is the row disappearing. `StatCard` renders as a button only when interactive, so an informational tile never advertises an action it lacks. The vacancy queue's bed count was cross-checked live against the Home card's own figure and matches exactly. **Phase 4 (one merged daily task list) is not built** — the shared component is the precondition for it.

### Intelligent Collection Queue — the owner's "where do I start" entry point
- **Status:** shipped 2026-08-05 — **Phase 2 of 4** · see [[Decisions#ADR-045|ADR-045]]
- **Owner-facing?** yes · **Tenant-facing?** no
- **Route:** `/owner/money/collect`, reached from the Action Center's Collect Rent card and from All Actions.
- **Key files:** Backend — `lib/services/collection-queue/{prioritisation.ts,collection-queue-service.ts}` (new), `app/api/owner/collection-queue/route.ts` (new), `tests/collection-queue-prioritisation.test.ts` (new, 37 pure tests). Frontend — `features/owner-collection/{collectionQueue.ts,collectionQueue.test.ts,useCollectionQueue.ts,CollectionQueuePage.tsx}` (new; 27 tests), `features/owner-dashboard/components/OwnerHomeDashboard.tsx`, `platforms/owner/router/OwnerRoutes.tsx`, `lib/queryKeys.ts`, `features/owners/api/index.js`.
- **What's real:** every tenant who owes money is bucketed into **Needs immediate attention · Due today · Waiting after reminder · Due soon** and ranked within the bucket. Each card shows outstanding, days overdue, last payment, last reminder, room and hostel, with Collect / Call / WhatsApp inline and the tenant profile a tap away.
- **Explainability:** the top two scoring reasons sit on the card; tapping them opens a sheet listing every factor with its points and the total. The ordering is never a black box.
- **Depends on:** `financialService.getTenantPaymentSummary`, `isOverdue()`, `reminder_logs` — **no new financial calculation**.
- **Notes:** Prioritisation is pure and takes `today` as a parameter, so it runs under `npm run test:pure` — which matters while the backend suite still has no test database. `recommendation: null` is on the contract from day one so the (unbuilt) recommendation engine won't require redesigning the page.

  Live data caught a flaw in the reminder-cooldown rule before ship, and confirmed the fix afterwards — see [[Decisions#ADR-045|ADR-045]]. Post-fix the queue reads 4 / 6 across "needs attention" / "waiting" (was 3 / 7), with the 11-day-overdue tenant correctly promoted, and the ₹84,500 total unchanged.

### Universal Search — the owner's "I know who I need" entry point
- **Status:** shipped 2026-08-05 — **Phase 1 of 4** · see [[Decisions#ADR-044|ADR-044]]
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** Backend — `lib/services/search/{types.ts,ranking.ts,search-service.ts}` + `providers/{tenant,hostel,room}-provider.ts` (all new), `app/api/owner/search/route.ts` (rebuilt), `tests/search-ranking.test.ts` (new, 22 pure tests). Frontend — `features/owner-search/{searchActions.ts,searchActions.test.ts,useUniversalSearch.ts,UniversalSearchOverlay.tsx}` (all new; 23 tests), `features/owner-dashboard/components/OwnerHomeDashboard.tsx`, `features/owner-dashboard/quick-actions/useHomeQuickActions.ts`, `features/hostel-drilldown/pages/HostelRoomsPage.tsx`, `features/owners/api/index.js`.
- **What's real:** searches tenants (name, phone ×4 fields, room, hostel, roll number, email, plus the word "invited"), hostels (name, city) and rooms (number) in one grouped, ranked response. Tapping a result goes straight to the tenant profile / hostel overview / that room's sheet.
- **Quick actions, inline:** Call · WhatsApp · Copy number · Collect · Profile — without opening the profile first. Collect drops into the existing QuickCollect flow with the tenant already selected.
- **Notes:** The Home search bar was previously a `<div>` wrapping a `<span>` — it looked like a field and did nothing. Two overlapping tenant-search endpoints already existed; one was orphaned and became this, the other (`/api/payments/quick-collect/search`) was deliberately left alone because its per-tenant ledger fetch suits a payment flow and not a typeahead.

  **Extensibility is the design constraint, not a bonus.** Sources are providers registered in one array; the route and the client never branch on a result type, and each result carries its own `href`. Payments, complaints, expenses, receipts and staff can be added by writing a provider alone.

  Actions are offered only when they can work — no Call without a number, no WhatsApp for a number whose country code can't be inferred, no Collect for an invited tenant or a zero balance. Verified live: `9845013001` finds a tenant stored as `+919845013001` as an exact match; `B-101` returns both its occupants and the room itself, grouped separately.

  **Phases 2–4 are not built** — the Action Center still shows counts, not work queues.

### Billing policy — the single screen that configures billing behaviour
- **Status:** shipped 2026-08-05 — see [[Decisions#ADR-043|ADR-043]]
- **Owner-facing?** yes · **Tenant-facing?** no (the policy governs what the owner may collect; tenants see only the resulting dues)
- **Key files:** Frontend — `features/owner-more/billing-policy/{MoreConfigBillingPolicyPage.tsx,billingPolicy.ts,billingPolicy.test.ts}` (new; 21 tests), `features/owner-tenants/quick-collect/QuickCollectModal.tsx`, `features/owner-more/pages/{MoreSettingsPage,MoreConfigFinancePage,MoreConfigHostelPage,MoreConfigReceiptFooterPage}.tsx`, `platforms/owner/router/OwnerRoutes.tsx`. **Deleted:** `MoreBillingPage.tsx`, `MoreConfigLateFeesPage.tsx`, `MoreConfigTenantDefaultsPage.tsx`. Backend — `lib/services/hostel-policy-service.ts`, `lib/preferences.ts`, `src/services/payments/{financial-policy,settlement-planner}.ts`, `tests/settlement-planner-minimum-percentage.test.ts` (new, 17 tests), `vitest.pure.config.ts` (new).
- **Route:** `/owner/more/configuration/finance/billing-policy`. Three old routes redirect to it.
- **Sections:** Rent collection (full vs part payments, minimum amount, minimum percentage) · Security deposit default · Agreement duration default · Rent schedule (generation/due/grace) · Late fee (flat/percentage/per-day + cap).
- **Depends on:** the existing `GET`/`PATCH /hostels/:id/preferences` — **no new endpoint was needed**.
- **Notes:** Replaces three screens that each owned a slice of billing and could overwrite one another; the old "Rent and billing" screen silently rewrote a PERCENTAGE late fee to FLAT and dropped its cap (see [[Bugs]]). The page always writes the **full** late-fee shape for exactly that reason.

  **The collect flow was redesigned around it.** The policy is stated before the owner types an amount ("Full payment only" / "Part payments allowed", with the floor spelled out and a Change link), and at review the sheet **either** explains why the payment can't proceed — with buttons to change the amount or open Billing policy — **or** describes the outcome in plain language ("₹100 will be collected", "₹7,900 will remain outstanding"). Previously it showed both at once. All copy is generated by pure functions in `billingPolicy.ts`, with tests asserting that no backend flag name (`allow_partial_payments`, `minimum_amount`, `payment_policy`) can leak into owner-visible text.

  Scoped to the owner's primary hostel, like the screens it replaces — a hostel picker here is still **open work** for multi-hostel owners.

### Owner Home property ordering — manual drag plus metric sorting
- **Status:** shipped 2026-08-04 — see [[Decisions#ADR-042|ADR-042]]
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** Backend — `lib/services/hostel-order-service.ts` (new), `app/api/owner/hostels/reorder/route.ts` (new), `lib/services/portfolio-service.ts`, `prisma/migrations/20260804120000_hostel_display_order/` (new), `tests/hostel-order-service.test.ts` (new, 8 cases — **written but never executed**, see below). Frontend — `features/owner-dashboard/property-order/{hostelSort.ts,hostelSort.test.ts,PropertyList.tsx,PropertySortControl.tsx,useHostelOrder.ts}` (all new; 22 tests), `shared/ui-patterns/DragHandle.tsx`, `features/owner-dashboard/components/{OwnerHomeDashboard,HostelOptionsSheet}.tsx`, `features/owner-dashboard/hooks/useOwnerDashboard.ts`, `features/owners/api/index.js`, `shared/mocks/dashboard.ts`.
- **Depends on:** [[APIs]] (`PATCH /api/owner/hostels/reorder`), [[Database]] (`hostels.display_order`), `motion` (already a dependency).
- **What's real:** five sort modes — **My order** (default) · Most dues · Most vacant · Top revenue · Name. Manual drag works in "My order" only, from the handle only, and persists server-side so the arrangement follows the owner across devices. Move up / Move down in the ⋮ sheet do the same thing without a pointer.
- **Notes:** This replaced a handle that had never worked — see [[Bugs]] for why (`react-dnd` present but imported nowhere, no `DndProvider`, decorative `aria-hidden` span, no ordering column). Sort labels are deliberately directional ("Most vacant", not "Occupancy") because occupancy sorts *ascending* and a bare metric name wouldn't say which end of the list you get.

  **The handle is hidden in the four metric modes.** Hand-ordering a metric-sorted list is incoherent, and hiding the affordance explains why it can't be dragged rather than leaving a dead control — precisely the failure being fixed. `DragHandle`'s three other call sites (floor groups, room layout, food-poll options) intentionally stay decorative, because those reorder behaviours also don't exist yet.

  **Not verified by automated backend tests.** `DATABASE_URL_TEST` is defined nowhere and `.env.test` doesn't exist, so the backend suite is unrunnable in this environment — pre-existing, confirmed by an untouched existing test failing identically. The endpoint was verified live against the real API instead. Provisioning a test database is open work.

### Owner activation visibility — the owner can see where every invited tenant is stuck
- **Status:** shipped 2026-08-01 — **activation business logic unchanged**
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** Backend — `src/services/tenants/activation-workflow-service.ts` (`getOwnerActivationState`, new read-only method), `app/api/tenants/[id]/activation-state/route.ts` (new), `tests/owner-activation-visibility.test.ts` (new, 13 tests). Frontend — `features/owner-tenants/activation/activationProgress.ts` + `.test.ts` (new, 25 tests), `activation/ActivationStepTracker.tsx` (new), `pages/PendingActivationsPage.tsx` (new), `hooks/useActivationState.ts` + `hooks/usePendingActivations.ts` (new), `pages/TenantDetailPage.tsx`, `features/owner-onboarding/pages/OwnerDashboardPreviewPage.tsx`, `platforms/owner/router/OwnerRoutes.tsx`, `features/tenants/api/index.js`.
- **Depends on:** [[APIs]] (`GET /api/tenants/:id/activation-state`), the existing activation state machine.
- **Notes:** The backend activation workflow was audited and found **complete and correct** — allocation created, reservation released, invitation ACTIVATED, tenant ACTIVE, all in one locked transaction. Nothing about it changed. What was missing was owner-side visibility: Tenant Detail showed only an "Invited" pill, and Home's "Activate Tenants" card (counting real invited tenants) opened the **Invite** wizard.

  **The key constraint:** reuse had to happen one level below `getContext()`. That method is keyed by the tenant's secret activation token *and mutates* — `markOpened: true` plus **auto-accepting the hostel rules on the tenant's behalf**. An owner opening a progress screen would have completed a step for the tenant. `getOwnerActivationState(tenantId, ownerId)` therefore loads the same records owner-scoped and calls the same private `computeState()`, with no writes on any path — three tests assert exactly that.

  The frontend reads `current_step` / `completed_steps` / `blocked_steps` / `progress_percent` as given and never re-derives a step; the percentage is read rather than recomputed so a backend reweighting propagates automatically. **KYC remains an independent state machine**, rendered as its own badge and explicitly tested to confirm unverified documents don't block reaching `ACTIVATE`. Tenants leave the queue the instant `activation_completed` is true, without waiting for the list's status string.

  **Verification limit:** no DOM harness — the step display and queue were not exercised in a browser this session.

### Owner app dead-button pass — Add hostel, hostel options, Tenant Actions, Room edit
- **Status:** shipped 2026-08-02 — frontend only, no backend changes
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `features/owner-onboarding/pages/{OwnerDashboardPreviewPage,OwnerOnboardingWizard}.tsx`, `features/owner-onboarding/hooks/useOwnerOnboardingState.ts` (new `initialStep` param), `features/owner-dashboard/components/{OwnerHomeDashboard,HostelOptionsSheet}.tsx` (new file), `features/owner-dashboard/quick-actions/AllActionsSheet.tsx`, `features/owner-tenants/actions/TenantActionsSheet.tsx`, `features/owner-tenants/pages/TenantDetailPage.tsx`, `features/hostel-drilldown/{room-sheet/RoomSheetModal,hooks/useHostelRooms,pages/HostelRoomsPage}.tsx`, `features/owner-more/pages/{MoreSettingsPage,MoreAboutPage,MoreHelpPage,MoreHostelIdentityPage}.tsx`, `features/settings/settingsHooks.ts` (new `useArchiveHostel`), `features/owners/api/index.js` (new `archiveHostel`), `platforms/owner/router/OwnerRoutes.tsx` (new `/owner/more/hostel/:hostelId` route).
- **Depends on:** [[Bugs]] (full root-cause writeup), the pre-existing `/onboarding` wizard, `DELETE /hostels/:id`, `ChangeFrequencyModal`, `CreateObligationModal`, `useTenantActions`, `ChangeRequestDrawer`, `roomService.update` — all already real and used elsewhere before this pass.
- **Notes:** User-reported: couldn't add a hostel, couldn't use the property card's "options," and "so many things" opened Coming Soon. None of this needed new backend work — every placeholder either had a working backend route with no frontend caller, or a working UI built for a different screen that had never been reused here. "+ Add hostel" → `/onboarding`; an already-authenticated owner (adding a 2nd/3rd hostel, not signing up) now skips Welcome/Account/KYC straight to naming the hostel (`initialStep` floor on `useOwnerOnboardingState`, clamped so Back can't reach the signup steps). The property card's kebab menu — the likely actual cause of "couldn't click the card option," since the card body itself already navigated correctly — now opens `HostelOptionsSheet`: "Edit hostel details" goes to `MoreHostelIdentityPage`, now mountable at `/owner/more/hostel/:hostelId` so it edits the tapped card's hostel specifically rather than always the owner's primary one; "Archive hostel" is a new `useArchiveHostel` mutation against the pre-existing, previously-unwired `DELETE /hostels/:id` (server-side blocked if the hostel still has active allocations). `TenantActionsSheet` — previously only "Receive Payment"/"Change Rent"/"Check-out" were real (see the Tenants-tab entry above, now superseded on this point) — now wires Share Payment Link, Create Charge, View Receipts, Request Change, and Change Billing Frequency, each reusing a component or hook already built and used on a different owner screen (`ChangeFrequencyModal`, `CreateObligationModal`, `useTenantActions().sharePaymentLink`, the tenant's own Activity tab, `ChangeRequestDrawer`) rather than new UI. Room Sheet's "Edit room details" now calls a new `updateRoom` mutation on `useHostelRooms` (`PATCH /rooms/:id`) for room number + rent — bed count is deliberately not editable here since it can't safely drop below occupied beds. Also wired: Settings' Tenant Defaults row (→ the real Configuration Hub page), About's Privacy Policy/Terms of Service (real `/legal/*` pages) and Licenses (new static OSS-attribution sheet, no backend needed), Help's Email us (`mailto:`), and the dashboard's "View Leads" quick action — relabeled "Invite Tenant" and wired to the real invite wizard, since no real lead-browsing surface exists for an owner and inviting a tenant is the actual real action that fills a vacant bed. **Deliberately left as honest `stayoToast.info('Coming soon')`, not force-fixed:** the Alerts tab's Leads/Renewals/Requests row actions (that tab's own list is still mock data — a bigger fix than wiring a button), "Review Agreements"/"Send Rent Reminders" on the dashboard's All Actions sheet (only aggregate counts are real; no per-item review page or bulk-reminder trigger exists yet), Contact Support/Report a Bug (no ticketing backend exists), and Food Polls (no persistence layer exists).

### Owner KYC verification — the owner can finally approve or reject a tenant's documents
- **Status:** shipped 2026-08-01 — **frontend only, zero backend changes**
- **Owner-facing?** yes · **Tenant-facing?** indirectly (they get approved, or told exactly what to fix)
- **Key files:** `features/owner-tenants/documents/kycDocuments.ts` (new — pure: status mapping, rejection-thread parsing, actionability, pending grouping, route builder), `documents/kycDocuments.test.ts` (new, 30 tests), `documents/DocumentReviewCard.tsx` + `documents/RejectDocumentSheet.tsx` (new), `pages/PendingVerificationsPage.tsx` (new), `hooks/useDocumentVerification.ts` + `hooks/usePendingVerifications.ts` (new), `hooks/useTenantDetail.ts` (now carries `download_url` + the rejection thread), `pages/TenantDetailPage.tsx`, `features/owner-dashboard/quick-actions/AllActionsSheet.tsx`, `features/owner-onboarding/pages/OwnerDashboardPreviewPage.tsx`, `platforms/owner/router/OwnerRoutes.tsx`. Backend: `tests/tenant-document-verification.test.ts` (new, 17 tests) — **coverage only, no behaviour change**.
- **Depends on:** [[APIs]] (`PATCH /api/tenants/:id/documents/:docId/verify` · `/reject` · `GET …/download` · `GET /api/tenants/pending-documents`), [[Bugs]].
- **Notes:** Fixes audit item **P0-4**. The audit found tenants could upload KYC that **nobody could ever approve** — onboarding stopped dead at that step. The cause was not a missing backend: `verify`, `reject`, `download` and `pending-documents` all existed, owner-scoped and correct, and `tenantService.verifyDocument/rejectDocument` already wrapped them with the right `PATCH` verb. What was missing was any UI reaching them — `VerificationPanel.tsx` had been left orphaned by the 2026-07-26 salvage pass. **So this task built no endpoints; requirement was to audit first, and the audit said don't.**

  Tenant Detail → Documents is now a real review surface (View · Download · Approve · Reject per document, plus the last rejection reason shown back to the owner). Rejecting **requires a reason** — the route already answered 400 without one, and the reason is the only thing the tenant is shown, so the sheet offers five one-tap common reasons plus free text and keeps Submit disabled until it is valid. New `/owner/tenants/verifications` queue, grouped **by tenant** rather than by document (an owner decides about a person) and ordered **oldest wait first** (that tenant's move-in is the one blocked). Home's "Verify Pending KYC" card — which already counted real documents and did nothing on tap — now navigates there.

  Approving invalidates **four** query keys together (`owner.pendingDocuments`, the tenants list, `portfolio.all`, and that tenant's detail), because approving the last required document flips `tenants.document_verified`, which Tenant Detail, the Tenants list, the Home count and the bell badge all derive from independently; refreshing three of four would leave a visibly stale number.

  **Deliberately not built:** *upload on behalf of the tenant*. It appears in the problem statement but not in the numbered requirements or acceptance criteria, and `/api/tenants/:id/documents` is **GET-only** — there is no owner-side upload endpoint, so it needs a new route plus ImageKit handling. Flagged rather than smuggled in.

  **Already working, verified not rebuilt:** the tenant sees the rejection reason at `/tenant/profile/details` — `TenantProfilePortalPage.tsx` already parses the same JSON thread the reject route writes.

  **Verification limits:** the 30 frontend tests cover pure logic (status mapping incl. never defaulting unknown → verified, thread parsing incl. malformed JSON and the legacy plain-string form, actionability matching the route guards, grouping/ordering/empty-queue, route building). The 17 backend tests are **characterisation tests** — they passed on first run because they pin behaviour that was already correct (cross-owner 403, tenant-role 401, archived 409, missing/whitespace/over-length reason 400, thread append not overwrite, `document_verified` flipped only when every required type is approved, `file_url` never leaked). No DOM harness, so the approve → refresh cycle was not exercised in a browser this session.

### Tenant invitation delivery — the wizard stops claiming success it can't verify
- **Status:** shipped 2026-08-01 — frontend only, **no backend change**
- **Owner-facing?** yes · **Tenant-facing?** indirectly (they now actually receive the invite, or the owner gets a way to send it by hand)
- **Key files:** `features/owner-tenants/invite/inviteDelivery.ts` (new — pure resolver + copy/share helpers), `invite/inviteDelivery.test.ts` (new, 30 tests), `invite/InviteDeliveryResult.tsx` (new — the 3-case result screen), `hooks/useInviteWizard.ts` (consumes the response; adds `addEmail → resend`), `invite/InviteTenantWizard.tsx`, `invite/steps/TenantStep.tsx` (optional email field), `invite/steps/VerifyStep.tsx`, `types.ts` (`tenantEmail`). Backend: only a type/doc-comment widening on `invitationService.resendInvitation`'s `overrides` (`email?: string` was already honoured at runtime). Plus `apps/frontend/vitest.config.ts` + `npm test` — **this app had no test suite before**.
- **Depends on:** [[APIs]] (`POST /api/owners/invitations`, `POST /api/tenants/resend-invitation`), [[Bugs]].
- **Notes:** Fixes audit item **P0-1**. `useInviteWizard.onSuccess` ignored the entire response and rendered *"Invitation sent! {name} will get a text to complete KYC."* on every 2xx — while the backend was already reporting `whatsapp_sent` / `whatsapp_error` / `email_sent` / `needs_email` and returning the `activation_link`. The wizard also collected **no email**, so the backend's email fallback could never fire. Realistic production outcome: an owner invites 20 tenants, sees 20 success screens, and nobody receives anything.

  **The trap that made this easy to get wrong twice:** both endpoints report delivery *failure* with a **2xx** status. `POST /api/owners/invitations` answers `202` when neither channel sent (`apiResponse(result, (whatsapp_sent || email_sent) ? 201 : 202)`), and `POST /api/tenants/resend-invitation` answers `202` with an *error-shaped* body (`{error:{code:'EMAIL_FALLBACK_REQUIRED'}}`). Axios resolves on 2xx, so neither ever rejects — a `try`/`catch` reads both as success. All delivery state is therefore read from the body, and anything unrecognised resolves to **undelivered**, never delivered (`whatsapp_sent: "false"` as a string does not count as sent).

  Three result states: WhatsApp delivered → "Invitation sent successfully"; WhatsApp failed but email delivered → "Invitation sent by email", naming the address; neither → "Invitation could not be delivered" with the reason, the activation link on screen, and **Copy link / Share link** (a `wa.me` deep link) so the owner is never stranded. When `needs_email` is true the result screen prompts for an address inline and re-dispatches via the existing resend route — no restart, no new backend surface. The resend's error body carries no `activation_link`, so the original is carried forward rather than lost.

  **Verification limits, stated plainly:** the 30 tests cover the pure decision logic (all three delivery cases, `needs_email`, the 202 error-body reading, link preservation, clipboard success/denial/absence, share-URL building). There is **no DOM test harness** — `apps/frontend` had no test suite at all, and adding jsdom + testing-library was judged a larger footprint than the fix; components are thin renderers over tested state. The live WhatsApp-fails → email-succeeds path was **not exercised against real providers** this session. See [[Changelog]] for the manual verification steps.

### Atomic hostel provisioning — onboarding publish is one transaction
- **Status:** shipped 2026-08-01 ([[Decisions#ADR-040|ADR-040]]) — supersedes the publish half of "real signup/onboarding backend integration" below
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** Backend — `lib/services/hostel-provisioning-service.ts` (new), `app/api/owner/hostels/provision/route.ts` (new), `src/validators/hostels/index.ts` (new — `HostelProvisionSchema`, `HOSTEL_TYPES`), `prisma/schema.prisma` + `prisma/migrations/20260801100000_hostel_provisioning_fields/` (3 columns on `hostels`), `lib/services/hostel-billing-preferences-service.ts` (exports `sanitizeBillingDefaultsPayload` for transaction-aware reuse), `tests/hostel-provisioning-service.test.ts` (new, 30 tests). Frontend — `features/owner-onboarding/api/onboardingApi.ts` (3 methods → `provisionHostel`), `hooks/useOnboardingSubmission.ts` (`submitPublish` is one `await`), `hooks/useOwnerOnboardingState.ts` + `hooks/onboardingValidation.ts` + `components/steps/DetailsStep.tsx` (the new required rent field).
- **Depends on:** [[APIs]] (`POST /api/owner/hostels/provision`), [[Database]] (`hostels.hostel_type`/`food_included`/`publish_requested`, `hostels.preferences_config.billing_defaults`, `rooms.base_rent`), [[Decisions#ADR-040|ADR-040]], [[Decisions#ADR-032|ADR-032]] (the lead lifecycle this must keep driving).
- **Notes:** Fixes audit item **P0-3**. Publish went from `1 + F + (F × R)` untransacted requests (45 for a 4×10 property, and a mid-flight failure left a committed half-hostel that the duplicate-name guard then made un-retryable *forever*) to one request run inside a single `prisma.$transaction`. Also fixes the four fields onboarding collected and threw away — hostel type, food, security deposit, publish-vs-draft — and gives every provisioned room a real `base_rent`, which required one new required field ("Starting monthly rent") on the Details step per explicit product direction; `null` base rent is rejected. **The Publish choice writes `publish_requested`, not `listing_status`** — see ADR-040 for why that distinction is a security boundary and not a modelling preference. Two defects were caught by the tests rather than by review: `z.coerce.number()` silently turning a `null` base rent into `0`, and the dropped `markHostelCreated` lead side effect that would have stalled every lead-originated owner at `OWNER_ACTIVATED`. **Not verified against a live database this session** — `npm test` requires `DATABASE_URL_TEST` (a separate test DB) which is unprovisioned here, so the 30 tests use this repo's established mocked-Prisma pattern; they pin that all writes go through the transaction client and that a mid-flight throw propagates, but real Postgres rollback and the migration itself still need one live run. `publish_requested` has no consumer yet.

### StayO owner app — real signup/onboarding backend integration (Landing → real OTP/signup → 12-step wizard → real hostel/floor/room creation → Dashboard)
- **Status:** shipped — real backend wiring, no manual database setup required to reach the dashboard. **Publish (step 11) superseded 2026-08-01** — see "Atomic hostel provisioning" above; the sequential hostel→floors→rooms loop described below no longer exists.
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** Backend — `app/api/auth/owner-signup/route.ts` (new), `authService.selfSignUpOwner()` (new, `lib/services/auth-service.ts`), `OwnerSignupSchema` (`lib/validators/index.ts`), `middleware.ts` (new public-route entry). Frontend — `features/owner-onboarding/api/onboardingApi.ts` (new), `features/owner-onboarding/hooks/useOnboardingSubmission.ts` (new), `features/owner-onboarding/router/RequireOwnerSession.tsx` (new), `features/owner-session/useOwnerSession.ts` (now actually consumed, previously built but unwired), `components/steps/AccountStep.tsx` (real password field, real OTP input), `pages/OwnerOnboardingWizard.tsx`, `router/OwnerJourneyRoutes.tsx`, `app/pages/public/LandingPage.tsx`.
- **Routes:** `/onboarding` is now ungated (real auth happens at its own account step, not before entry). `/get-started/home` and the rest of the owner-app tree are gated by the new `RequireOwnerSession` (real session + at least one real hostel — derived from the database, not a client flag).
- **Depends on:** [[APIs]] (`/api/auth/owner-signup`, `/api/auth/send-phone-otp`+`/verify-phone-otp`, `/api/owner/hostels`, `/api/floors`, `/api/rooms`), [[Decisions]] ADR-028, [[Backend]] (Supabase Postgres connectivity, provisioned this session).
- **Notes:** The account step (step 2 of 12) now does real work: send OTP → user enters the real 6-digit code (was decorative dots before) → verify OTP → `POST /auth/owner-signup` creates a real `profiles` row → `AuthContext.login()` hydrates the app's real, already-mature session system (idle timeout, refresh, `/auth/me` — see [[Frontend]]) so no second/parallel session mechanism was needed. Publish (step 11) creates a real `hostels` row, then one `floors` row per the wizard's floor count, then one `rooms` row per floor × rooms-per-floor (capacity = beds-per-room) — all via already-existing, previously-audited-ready backend endpoints, no new backend work needed there. KYC/Create/Location/Details/Floors/Rooms/Beds/Review steps (3–10) are unchanged, still local wizard state, matching the existing Review-then-Publish shape. Verified end-to-end at the API level (OTP send/verify, signup, login, hostel/floor/room create, using the exact request/response shapes `onboardingApi.ts` sends) against the live database with a fresh test account (`real-owner-flow@stayo.dev`); a full browser click-through was not independently driven this session (no browser-automation tool available in this environment) — manual verification: run both dev servers, visit the landing page, click an owner CTA, go through Google-mock → account step with a fresh email/phone/password → read the OTP from `apps/backend/latest-otp.txt` (or `.next/server/app/latest-otp.txt` once built) since WhatsApp isn't configured in this environment → continue through the wizard → Publish → confirm arrival at a populated `/get-started/home`. Next phase: replace Dashboard's mock data with live API calls (per the pre-existing backend-readiness audit), then Tenants, Money, Hostel.

### StayO owner-acquisition funnel — real lead capture → admin approval → activation (Landing → real Google + OTP lead → Admin Console review → activation link → prefilled onboarding → auto-progressing lead status)
- **Status:** shipped, real end-to-end (no mocks) — three passes: lead capture (2026-07-28), admin approval/activation (2026-07-29), WhatsApp-OTP system-failure fallback (2026-08-01)
- **Owner-facing?** yes (as a prospective owner) · **Tenant-facing?** no · **Admin-facing?** yes (the Leads review/approve side)
- **Key files:** Backend — `app/api/leads/self-serve/route.ts`, `app/api/leads/invitation/[token]/route.ts` (+`/complete`), `app/api/platform-admin/leads/[id]/approve/route.ts`, `src/services/platform-leads/lead-invitation-service.ts`, `lib/services/notifications/providers/whatsapp/meta-provider.ts`'s `sendOwnerActivation`, `lib/services/email-service.ts`'s `sendOwnerActivation`, `app/api/auth/owner-signup/route.ts` (+`lead_token`), `app/api/owner/hostels/route.ts` (+`HOSTEL_CREATED` side effect). Frontend — `shared/ui-patterns/GoogleSignInModal.tsx` (now real), `app/pages/public/LeadSignupCallbackPage.tsx`, `features/owner-onboarding/components/HostelLeadModal.tsx` (now real), `features/hostel-leads/api/index.ts`, `app/pages/public/OwnerLeadInvitePage.tsx` (new), `platforms/admin/pages/AdminLeadsPage.tsx`, `features/owner-onboarding/hooks/useOwnerOnboardingState.ts`/`useOnboardingSubmission.ts` (prefill + token threading).
- **Routes:** `/` (landing CTA) → `/lead-signup/callback` (real Google identity, ephemeral — never becomes a session) → real `HostelLeadModal` (details + phone OTP, `purpose: LEAD_CAPTURE`) → `platform_leads` row created (`NEW`). Admin Console `/admin/leads` → "Approve Lead" → `/owner-invite/:token` (public, distinct from the *tenant* activation route at `/invite/:token`) → `/onboarding`, prefilled via router state.
- **Depends on:** [[APIs]] (`/api/leads/*`, `/api/platform-admin/leads/[id]/approve`), [[Database]] (`platform_lead_invitations`, `PlatformLeadStatus`, `platform_leads.verification_status`), [[Decisions]] ADR-032, ADR-031 (Google identity plumbing this builds on).
- **Notes:** Full lifecycle is `NEW → UNDER_REVIEW → APPROVED → INVITE_SENT → OWNER_ACTIVATED → HOSTEL_CREATED → LIVE` (or `LOST`); everything from `APPROVED` onward is system-managed (the admin PATCH endpoint rejects manual writes to those values — see ADR-032). The security gate for lead creation is the phone OTP, not the Google step (which is a frontend-only identity/anti-spam gate, deliberately never resolved into a StayO session, since `resolveSupabaseSession()` is built to reject exactly that "no account yet" case by design). **Known environment gaps, not code bugs**, verified live: WhatsApp delivery needs a new Meta-approved template (`WHATSAPP_OWNER_ACTIVATION_TEMPLATE`) that doesn't exist yet outside this codebase; the configured `RESEND_API_KEY` in this dev environment is invalid, so the email fallback also currently fails — both correctly fall through to leaving the lead at `APPROVED` with the delivery error surfaced, rather than lying about a successful send. The rest of the pipeline (activation-context fetch, prefill, real owner-signup with `lead_token`, hostel creation, publish-complete) was verified live end-to-end using a manually-seeded valid token to work around the send failure, confirming every downstream step independent of those two external credentials. **2026-08-01:** given the WhatsApp delivery gap above, a lead-*capture*-time OTP send can genuinely fail in this environment too — `HostelLeadModal.tsx` now falls back to creating the lead unverified (`verification_status: SKIPPED_SYSTEM_FAILURE`) rather than dropping the submission; see [[Business-Rules]] and [[Bugs]] for the full rule and the exact failure it fixed.

### StayO owner app — Tenants tab (list, Invite wizard, Tenant Detail, Quick Collect)
- **Status:** shipped (mock data / local state only — no backend wiring yet)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `shared/mocks/{dashboard,tenants,payments}.ts` (centralized mock data, reused across features — not per-feature); `features/owner-tenants/` — `types.ts`, `hooks/{useTenantFilters,useInviteWizard}.ts`, `components/{TenantList,TenantRow,TenantFilters}.tsx`, `invite/InviteTenantWizard.tsx` + `invite/steps/{TenantStep,StayStep,MoneyStep,VerifyStep}.tsx`, `quick-collect/QuickCollectModal.tsx`, `actions/TenantActionsSheet.tsx`, `pages/{TenantsPage,TenantDetailPage}.tsx`; `app/layouts/OwnerAppShell.tsx` (new `basePath` prop).
- **Routes:** `/get-started/tenants` (list, inside `OwnerAppShell basePath="/get-started"`), `/get-started/tenants/:tenantId` (detail — a **real route**, not a modal, deliberately, so it can become `/owner/tenants/:tenantId` later with minimal changes; mounted outside `OwnerAppShell` as a full-screen takeover with its own back button, matching the design).
- **Depends on:** [[Frontend]], the owner acquisition journey above (same `MockOwnerJourneyContext` gating, same `RequireMockOwnerJourney require="activated"`).
- **Notes:** Full experience per explicit direction — list (hostel filter/search/status chips) + the complete 4-step Invite Tenant wizard (Tenant → Stay → Money → Verify, `Stayo App.dc.html` lines 1747–1970) + Tenant Detail (profile, Communication Center, Risk & Compliance, Private Notes, money stat cards, 4 tabs: Charges/Activity/Documents/Stay, lines 2178–2385) + Tenant Actions sheet (lines 2385–2429) + 5-step Quick Collect (Select Tenant → Amount/Settlement → Preview → Password → Success, lines 1998–2178). Quick Collect's settlement math (`shared/mocks/payments.ts::computeSettlementPreview`) is simple client-side summing, not the real FIFO engine. In `TenantActionsSheet`, only "Receive Payment" is wired to open Quick Collect — every other row (Share Payment Link, Create Charge, View Receipts, Request Change, Change Rent, Change Billing Frequency, Check-out/Exit) shows an honest `stayoToast.info('Coming soon')` instead of a silent no-op. Fixed a real bug while building this: `OwnerAppShell`'s bottom nav was hardcoded to `/owner/*`, so the mock dashboard preview's nav tabs bounced to `/` (no real session, real `ProtectedRoute` redirect) — added a `basePath` prop, default `/owner` unaffected, mock preview passes `/get-started`. Real `/owner/tenants` (`OwnerRoutes.tsx`) is untouched, still a `RouteScaffold` — same precedent as `/owner/home`. **Both since superseded**: `/owner/tenants`/`/owner/home` are real, live routes (not `RouteScaffold`) as of the 2026-07-26 owner-app convergence, and `TenantActionsSheet`'s remaining rows were wired to real flows in the 2026-08-02 dead-button pass — see that entry above.

### StayO owner app — Hostel Drill-down (Overview, Rooms grid/layout, hostel-scoped Tenants)
- **Status:** shipped (mock data / local state only — no backend wiring yet)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `shared/mocks/rooms.ts` (new — `Floor`/`Room`/`RoomBed`, `getHostelRoomStats()` derives stat tiles from the room list); `features/hostel-drilldown/` — `types.ts`, `hooks/useRoomsLayout.ts`, `components/{BedStatusDots,RoomRow,RoomLayoutCard,FloorGroup}.tsx`, `room-sheet/RoomSheetModal.tsx`, `add-room/AddRoomModal.tsx`, `add-floor/AddFloorModal.tsx`, `layout/HostelDrilldownLayout.tsx`, `pages/{HostelOverviewPage,HostelRoomsPage,HostelTenantsPage}.tsx`.
- **Routes:** `/get-started/hostels/:hostelId/{overview,rooms,tenants}` — the identical path shape to the real, still-scaffolded `OwnerRoutes.tsx` routes (`/owner/hostels/:hostelId/{overview,rooms,tenants}`), nested under `HostelDrilldownLayout` (back button, hostel name/status, 3-tab sub-nav), mounted outside `OwnerAppShell` as a full-screen takeover — same treatment as Tenant Detail.
- **Depends on:** [[Frontend]], the Tenants tab above (`HostelTenantsPage` reuses its `TenantList`/`TenantRow`, and tapping a tenant lands on the same `/get-started/tenants/:tenantId` page).
- **Notes:** Reached by tapping a property card on Home (`OwnerHomeDashboard` had no click handler on those cards at all before this — added an `onSelectProperty?` prop). Rooms sub-tab (the part specifically requested) has two modes as local state, not separate routes, matching `Stayo App.dc.html` lines 1520–1592: browse (floor-grouped rows, 4-dot bed-status indicator, search, "+ Add") and layout/reorder (native HTML5 drag-and-drop to move rooms between floors, same DnD pattern the Home tab's draggable property cards already use — no new library). Room Sheet (lines 1626–1661), Add Room (1663–1705), and Add Floor (1707–1745) modals are all built. Scope note stated rather than hidden: Add Room/Add Floor's forms and live preview text work, but submitting just closes the modal — neither actually mutates the mock room list yet. Real `/owner/hostels/:hostelId/*` untouched, still `RouteScaffold`.
- **Follow-up same day**: the owner app's product-theme screens (`OwnerAppShell`, `TenantDetailPage`, `HostelDrilldownLayout`) were missing the graph-paper background grid the design sets on its root (`Stayo App.dc.html` line 31, `#EBDCCF` 1px lines / 52px grid) — the marketing-theme `LandingPage.tsx` had its equivalent from an earlier phase but the product-theme surfaces never did. Added to all three.

### StayO owner app — Money tab (Overview/Collections/Expenses, Add Expense wizard, Filters/Export/Detail)
- **Status:** shipped (mock data / local state only — no backend wiring yet)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `shared/mocks/expenses.ts` (new — `MockExpense[]`, `EXPENSE_CATEGORIES`, `PAYMENT_METHOD_OPTIONS`, `getExpenseInsights()` derives category breakdown/top vendor/largest expense/net profit); `features/owner-money/` — `types.ts`, `hooks/{useMoneyPage,useAddExpenseWizard}.ts`, `components/pulse/{StatusBanner,MoneyStatTiles,CollectionRateCard,ActionQueueCard,CashflowForecastCard}.tsx`, `components/collections/{TenantDueRow,CollectionsFilters}.tsx`, `components/expenses/{BusinessHealthStrip,InsightTilesGrid,MonthlyTrendCard,ExpenseSearchBar,CategoryChipsRow,ExpenseRow,ExpenseBreakdownCard,TopVendorsCard}.tsx`, `add-expense/AddExpenseModal.tsx` + `steps/{DetailsStep,FinancialStep,ReviewStep}.tsx`, `filters/ExpenseFiltersModal.tsx`, `export/ExportExpensesModal.tsx`, `expense-detail/ExpenseDetailModal.tsx`, `pages/MoneyPage.tsx`.
- **Routes:** single route `/get-started/money` (replacing its `RouteScaffold`), inside the same `OwnerAppShell basePath="/get-started"` wrapper as Home/Tenants — unlike Tenants/Hostel-Drilldown, nothing here needed a separate route, since the design itself treats Collections/Expenses as tab-switches and everything else as bottom-sheet overlays, not full screens.
- **Depends on:** [[Frontend]], the Tenants tab above (every "Collect" button reuses its `QuickCollectModal` rather than a new payment flow; `shared/mocks/tenants.ts` backs Collections/Action Queue directly).
- **Notes:** Full design source read (`Stayo App.dc.html` lines 267–866, the largest single tab so far). `TenantDueRow` and `MoneyStatTiles` are shared between Pulse and Collections/Expenses since the design reuses the identical row/tile shape rather than each screen re-inventing it. Scope note stated rather than hidden: Add Expense's 3-step wizard works and computes a correct review total, but "Save expense" just closes the modal without mutating `shared/mocks/expenses.ts` — same for Expense Detail's Edit/Duplicate/Mark Pending/Delete (`stayoToast.info('Coming soon')`) and Export (`stayoToast.success` confirmation, no file generated). Real `/owner/money` untouched, still `RouteScaffold`.

### StayO owner app — Food tab (Monthly Planning & Voting, real backend; Food Polls unchanged/mock)
- **Status:** shipped — Monthly Schedule sub-tab rebuilt end-to-end on a real backend, replacing the old Week1-4 mock model. Food Polls sub-tab is untouched, still 100% mock, kept as a separate feature per explicit product decision. **⚠️ Partly superseded 2026-08-05 — the Food Polls sub-tab was deleted outright ([[Decisions#ADR-048|ADR-048]]); everything below about "Food Polls unchanged/mock" and the sub-tab toggle is historical. The Monthly Schedule half still stands. Current state: [[Food]].**
- **Owner-facing?** yes · **Tenant-facing?** yes (first real tenant-facing surface of the Food tab — new `/tenant/food` page)
- **Key files — backend:** `prisma/schema.prisma` (`food_menu_items`, `food_voting_periods`, `food_votes`, `food_schedules`, `food_schedule_meals` — see [[Database]]), `app/api/food/**` (~17 routes — menu-items CRUD, voting-period lifecycle + results, tenant voting, schedule generate/fetch/edit-cell/publish/history, tenant schedule + history — see [[APIs]]), `lib/services/food-schedule-generator.ts` (`assignWeekForMealType` — weighted round-robin generation from vote tallies, largest-remainder proportional slot counts, spreads repeats across the week rather than clustering them), `app/api/cron/food-carry-forward/route.ts` (new cron, see [[APIs]]).
- **Key files — frontend (owner):** `features/food/api/index.ts` (shared `foodService`), `features/owner-food/hooks/{useFoodMenuItems,useFoodVoting,useFoodSchedule,useFoodScheduleHistory}.ts` (replace deleted `useFoodLibrary`/`useMealSchedule`/`buildSchedule.ts`), `components/menu/{FoodLibraryCard,MealChip}.tsx` (rewritten for id-based items), `components/voting/VotingPanel.tsx` (new), `components/schedule/{WeeklyScheduleGrid,ScheduleMealPickerSheet,MonthHistoryList}.tsx` (new, replace deleted `MonthlyMenuCard`/`WeekGroup`/`MealCard`/`MealActionSheet`/`MealPickerSheet`), `pages/FoodPage.tsx` (rewritten — Monthly Schedule sub-tab now composes Library → Voting → Weekly grid → History; Food Polls sub-tab's components/hooks (`CreatePollModal`, `PollResultsSheet`, `useFoodPolls`, `mockFoodPolls`) are unchanged).
- **Key files — frontend (tenant, new):** `features/tenant-session/useTenantSession.ts`, `features/food/hooks/{useTenantFoodVoting,useTenantFoodSchedule}.ts`, `platforms/tenant/pages/TenantFoodPage.tsx` (replaces the `RouteScaffold` at `/tenant/food` in `TenantRoutes.tsx`).
- **Depends on:** [[APIs]] (`/api/food/*`), [[Database]] (5 new models/5 new enums), [[Decisions]] (vote-granularity + Food-Polls-coexistence ADRs), [[Business-Rules]].
- **Notes:** Replaces the prior Week1-4 mock model entirely — the owner no longer manually builds a Monday-Sunday timetable. Real flow: owner maintains a per-meal-type food-item library → opens monthly voting (start/end window) → tenants **vote for as many items as they like per meal type** (multi-select — reversed 2026-07-26 from an initial single-choice design, see [[Decisions]] ADR-029; casting a vote toggles one `(meal_type, item)` pair on/off) → owner clicks "Generate Schedule," which tallies votes and runs the weighted round-robin generator to produce a full 7-day × 4-meal grid (falls back to all active library items, name-ranked, for any meal type with zero votes, so no slot is ever left empty) → owner reviews/edits any cell (tap → picker sheet) → owner publishes (DRAFT→PUBLISHED). **Schedules are modeled as one mutable row per `(hostel_id, month)` representing a repeating weekly pattern (28 cells), not calendar-dated** — this is the key modeling decision, since it makes "publish" a status flip on the same row and means **any edit made after publishing takes effect immediately with no separate "republish" step**, matching the spec exactly. New months automatically carry-forward the previous month's published schedule as a `DRAFT`/`CARRIED_FORWARD` copy via the new daily cron (idempotent on `food_schedules`'s `@@unique([hostel_id, month])`) if the owner does nothing. Month history is browsable both owner- and tenant-side (published months only). Publishing fans out an in-app notification to every active, profiled tenant. **Known testing gap:** the only seeded tenant (Arjun Mehta) remains `INVITED`, not `ACTIVE`, so a real tenant-side vote-cast and published-schedule view were never exercised end-to-end with a live 200 response — all tenant-route correctness (403 for non-tenant tokens, response shape) was verified via curl with an owner token substituted for the auth check path, not a genuine tenant session. The carry-forward cron's "clone from previous month" branch was also not exercised live (no prior published month existed to carry from at build time; only the idempotent "already exists" no-op branch was verified) — assessed low-risk since it reuses the exact `createMany` pattern already verified correct in the generate-schedule route. Real `/owner/food` — Monthly Schedule sub-tab now fully real; Food Polls sub-tab intentionally untouched (kept as a separate, still-mock feature per explicit "keep both" product decision, not superseded). **Update, 2026-07-27:** `TenantFoodPage`'s menu display changed from a month-picker (one month's weekly grid shown at a time) to every published month shown at once, each as its own section (oldest → newest, most recent labeled "Current") with its Monday–Sunday cells laid out as a horizontally-scrollable row rather than stacked full-width day cards — explicit tenant-side UX request, no calendar/date grid. `useTenantFoodSchedule` now fans the existing `GET /food/tenant/schedule?month=` out across every month `GET /food/tenant/schedule/history` returns (parallel `useQueries`, no backend change) instead of fetching one selected month; still exposes a `grid` field (current month only) for `useTenantHome`'s "today's meals" lookup. See [[Changelog]].

### StayO owner app — Frontend Completion Pass (Alerts, More, FAB, returning-owner, desktop centering)
- **Status:** shipped (mock data / local state only — no backend wiring yet); declares the owner-app preview **feature-complete and frozen** ahead of backend integration
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `shared/mocks/{alerts,more}.ts` (new); `features/owner-alerts/` — `hooks/useAlerts.ts`, `pages/AlertsPage.tsx`; `features/owner-more/` — `hooks/useMoreNav.ts`, `components/MoreScreenHeader.tsx`, `pages/{MorePage,MoreSettingsPage,MoreBillingPage,MoreHelpPage,MoreAboutPage}.tsx`; `features/owner-dashboard/quick-actions/` — `useHomeQuickActions.ts`, `QuickActionsSheet.tsx`, `AllActionsSheet.tsx`.
- **Routes:** `/get-started/alerts` (Home's bell icon only, no bottom-nav icon — matches the design); `/get-started/more` + `/more/{settings,billing,help,about}` (all inside `OwnerAppShell`, bottom nav stays visible on every sub-screen, matching the design's own `moreIsSettings`/`moreIsBilling` behavior). `/more/help` and `/more/about` have no real-route counterpart yet in `OwnerRoutes.tsx`.
- **Depends on:** [[Frontend]], `shared/ui-patterns/ListRow` (backs every More/Settings/Alerts row), reuses `features/owner-tenants`'s `QuickCollectModal`/`InviteTenantWizard` and `features/owner-money`/`features/owner-food`'s existing Add Expense/Create Food Poll modals (via router-state auto-open, not duplicated flows) for the Home FAB.
- **Notes:** A full navigation audit (prompted by explicit request) found `/get-started/more` was a hard 404 despite the bottom nav linking to it, and that the bell icon, FAB, "View all", property-card kebab menu, and "+ Add hostel" were all unwired dead buttons — all fixed here, each either performing a real action or showing `stayoToast.info('Coming soon')`. Also found and fixed a returning-owner bug: `LandingPage.tsx`'s CTA claimed "Go to My Hostel" for any authenticated mock session but re-opened lead capture on click, since `journey.activate()` was never called by that flow — now calls it and navigates straight to `/get-started/home`. Also corrected an inaccurate earlier tracker claim: `Stayo App.dc.html` has **zero** `@media`/desktop rules (verified by grep) — there was no desktop design to "extract"; `OwnerAppShell`/`TenantDetailPage`/`HostelDrilldownLayout` instead center the existing mobile layout in a bordered `max-w-[480px]` frame on `sm:`+ viewports. Help & Support and About StayO are **not in the design source** — added per explicit request using the identical `ListRow`/card pattern, flagged honestly rather than presented as ported. "Workspace Configuration" (which in the design links to the wholly separate `Stayo Config.dc.html` app) stays a `stayoToast.info('Coming soon')`. Real `/owner/*` untouched.

### StayO owner app — Configuration hub, Phase 1 (real, per `Stayo SaaS redesign1/Stayo Config.dc.html`)
- **Status:** shipped, phase 1 of a deliberately-scoped multi-phase build — closes the gap flagged in the 2026-07-27 UI-fidelity pass's Notes above ("Workspace Configuration"... stays a `stayoToast.info('Coming soon')`, since the deep-link target didn't exist as a route)
- **Owner-facing?** yes · **Tenant-facing?** no
- **Key files:** `features/owner-more/pages/MoreConfigurationHubPage.tsx` (hub: search overlay, progress ring, needs-attention feed, module grid) + `hooks/useConfigurationHub.ts` (composes `useHostelPolicy` + `portfolioService.getSummary()` + `useHostelBedSummary.ts`, new); Hostel module — `pages/MoreConfigHostelPage.tsx`, `pages/MoreConfigTenantDefaultsPage.tsx`; Finance module — `pages/MoreConfigFinancePage.tsx`, `pages/MoreConfigLateFeesPage.tsx` (real 3-charge-type late fees — FLAT/PERCENTAGE/PER_DAY, supersedes `MoreBillingPage.tsx`'s FLAT-only toggle, which still exists at `/owner/more/billing`, unlinked from this new flow but not deleted), `pages/MoreConfigPaymentGatewayPage.tsx` (intentionally static — no self-serve gateway-connect flow exists), `pages/MoreConfigReceiptFooterPage.tsx`; shared `components/{ConfigProgressRing,ConfigModuleCard,ConfigAttentionRow,ConfigSearchOverlay,Stepper}.tsx`, `data/configSearchIndex.ts`.
- **Routes:** `/owner/more` (bottom-nav "Configure" tab's actual target) and `/owner/more/configuration` both render the hub; `/owner/more/configuration/{hostel,hostel/tenant-defaults,finance,finance/late-fees,finance/payment-gateway,finance/receipt-footer}`. All real-data sub-pages under `/owner/more/{settings,billing,profile,hostel,notices,service-requests,help,about}` are unchanged and now reachable via a new "Account settings" section folded into the bottom of the hub.
- **Depends on:** [[Backend]] (`hostel-policy-service.ts`'s `mergePolicy`/`VALID_DOMAINS` — the generic `PATCH /hostels/:id/preferences` already read/writes every policy domain including `automation`/`notifications`/`branding`, so this phase needed zero new backend endpoints), [[APIs]], reuses `MoreHostelIdentityPage.tsx`/`MoreScreenHeader.tsx`/`ListRow`/`Toggle` rather than duplicating them.
- **Notes:** Built against a research pass (not assumption) of which of the mockup's 6 modules have real backing data. Only **Hostel** and **Finance** ship this phase — Agreements/Automation/Notifications/Account & Team are deliberately **not** in the module grid at all (not even as "Coming soon" tiles), since they'd either need new backend work (agreement templates/clauses/e-signing has no DB model; multi-user/roles/2FA has no schema) or a real-but-different, coarser shape than the mockup shows (automation actually has 4 real toggles via `policy.automation` gating real cron jobs — confirmed live; notifications only has 2 coarse config groups, not the mockup's per-event×4-channel matrix). "Needs attention" and module status are computed from genuine gaps only — a deliberately-off toggle is never flagged, only real misconfiguration (e.g. late fee enabled with no amount) or missing required data. Live-verified against the real dev database via authenticated API calls (not just UI click-through): confirmed the live-stored policy for the real test hostel matched every field this reads, PATCHed the exact late-fee payload shape `MoreConfigLateFeesPage.tsx` sends and confirmed it persisted then reverted it, and caught a real bug this way — the Tenant Defaults screen's deposit-months stepper allowed 0, but the backend's `validateHostelPolicyForWrite` requires 1–12 (confirmed live: a `deposit_months:0` PATCH is rejected) — fixed before this note was written. Found (but did not touch) a second, unrelated `/owner/config/*` route tree (`ConfigRoutes.tsx`/`ConfigShell.tsx`) already scaffolded against the same design file, entirely `RouteScaffold` placeholders and unlinked from navigation — doesn't conflict with this work but should be reconciled with whoever added it rather than left as two parallel implementations.

### StayO tenant app — Home / Money / Room / Profile (real backend, per `Stayo SaaS redesign1/Stayo Tenant.dc.html`)
- **Status:** shipped — all 5 tabs real (`Food` was already real from the earlier Food feature)
- **Owner-facing?** partial (Notices + Service Requests queue, both new small owner-side surfaces) · **Tenant-facing?** yes
- **Key files — Home:** `features/tenant-home/hooks/useTenantHome.ts`, `platforms/tenant/pages/TenantHomePage.tsx`; composes `useTenantFinancials`, `useTenantFoodSchedule`/`useTenantFoodVoting` (already real from Food), `useTenantRoom` (complaint-status card), and `hostelContentService`/`tenantRoomService` for announcements/events/room.
- **Key files — Money:** `features/tenant-financials/{api,hooks/useTenantFinancials,components/PaySheet}.ts(x)` — shared by Home's rent-due hero and Money's own Pay buttons, so there is exactly one payment-flow implementation. `platforms/tenant/pages/TenantMoneyPage.tsx` salvages the real logic (financial-read-model, billing-timeline, billing-frequency, payment history, `paymentService.createIntent`→PhonePe-checkout-redirect, `paymentService.generatePayLink`) that previously only existed in the orphaned `src/portal/pages/TenantFinancialsPage.tsx`, restyled to the new design (the design mockup's own copy says "Secured by Razorpay" — corrected to PhonePe, the real provider).
- **Key files — Room:** `features/tenant-room/{api,hooks/useTenantRoom,components/ServiceRequestSheet}.ts(x)`, `platforms/tenant/pages/TenantRoomPage.tsx` — room snapshot, roommates (derived from `/api/tenants/me/room`, which already returned them), living-status grid (new `hostel_utility_status`), active-maintenance-ticket 4-step tracker, one generic request-form sheet driving all 6 service-request types, house-rules accordion (new `hostels.house_rules`). Owner side: `features/owner-more/pages/MoreServiceRequestsPage.tsx` (`/owner/more/service-requests`) — a queue to assign/progress/resolve/reject incoming requests, without which the tenant-facing form would be write-only.
- **Key files — Profile:** `platforms/tenant/pages/{TenantProfilePage,TenantHelpPage}.tsx` (new, matching the design's stub-shaped Profile tab — hero + 5 rows, each a real destination). "Personal details"/"Documents" salvage the already-real `src/portal/pages/TenantProfilePortalPage.tsx` (full profile edit, OTP-verified phone changes, KYC document upload, owner Q&A chat thread) mounted at `/tenant/profile/details`, unrestyled — a genuine 1345-line working component, not worth a from-scratch rebuild against a design source that only sketched this tab as a 4-row stub. "Plan move-out" mounts the already-real `src/portal/pages/TenantMoveOutPage.tsx` at `/tenant/move-out`. "Agreement & renewal" mounts `TenantRenewalPage.tsx` at `/tenant/renewal` — **previously fully built but never routed anywhere** (see [[Bugs]]), now reachable for the first time.
- **New minimal content models:** `hostel_announcements`, `hostel_events` (owner-authored, written via new `features/owner-more/pages/MoreNoticesPage.tsx` at `/owner/more/notices`).
- **Depends on:** [[APIs]] (Tenant App — Home/Room content, Food Planning & Voting), [[Database]], [[Decisions]].
- **Notes:** Follows the same salvage-first playbook as the owner-app convergence (see 2026-07-26 Changelog entries below) — real, working logic from the frozen `src/portal` tree and previously-orphaned `platforms/tenant` pages was mounted into the live route tree rather than rebuilt. Fixed a real, previously-undiscovered bug found while wiring this: `LoginPage.tsx`, `CompleteProfilePage.tsx`, and `ActivateAccountPage.tsx` all redirected a tenant post-login/activation to `/tenant/dashboard`, a route that stopped existing when `TenantRoutes.tsx` was rebuilt around `TenantAppShell` — every tenant login silently 404'd into the public landing page catchall. Now redirects to `/tenant/home`. Also fixed a dev-environment gap: `payment-service.ts` validates `RAZORPAY_KEY_ID`/`SECRET`/`WEBHOOK_SECRET` unconditionally at module import even though PhonePe is the real live provider, 500ing every route that imports `paymentService` locally — added dev-placeholder env vars matching the existing `.env` convention (see [[Backend]]). Verified end-to-end against the live dev database: activated the two previously-`INVITED` seed tenants (`profile` row + `ACTIVE` status + a real room allocation) to get a genuine tenant session for the first time this project — resolving a testing gap flagged repeatedly since the Food feature — and drove the full flow live: real financials/room/roommates/announcements/events/food data on Home, real score/billing-frequency/pay-link generation on Money, a full raise→assign→resolve service-request round-trip with timeline on Room, and real profile/renewal/move-out data on Profile. The one path not exercised end-to-end is the actual PhonePe checkout redirect itself, which needs real merchant gateway credentials this dev environment doesn't have — a genuine external-dependency limit, not a code gap (the same `createIntent` call path `TenantFinancialsPage.tsx` already used, unchanged).

### StayO Platform Admin Console (real, full multi-tenant SaaS platform admin — per `Stayo Admin Dashboard/Stayo Admin.dc.html`)
- **Status:** shipped — all 6 screens real (Dashboard, Hostels, Leads, Revenue, More, Settings)
- **Owner-facing?** no (owners are the *subject* of this console, not its users) · **Tenant-facing?** no · **New persona:** Platform Admin (`role: ADMIN`)
- **Key files — backend:** `prisma/schema.prisma` (`platform_admins`, `platform_leads`, `subscription_plans`, `hostel_subscriptions`, `platform_invoices`, `notification_templates`, `platform_settings`, plus `hostels.verification_status`/`listing_status` — see [[Database]]), `app/api/platform-admin/**` (~18 routes, see [[APIs]]), `scripts/bootstrap-platform-admin.ts` (one-time first-admin creation — `npm run bootstrap:platform-admin -- <email> <password> <name>`).
- **Key files — frontend:** `app/layouts/AdminAppShell.tsx` (sidebar chrome ≥900px per `Stayo Admin.dc.html`'s own breakpoint; below 900px the sidebar hides and a floating pill bottom-nav takes over — same `ADMIN_TABS` list drives both, matching the mockup's `@media (max-width:900px)` rules exactly. Fixed 2026-07-27 — the shell originally shipped desktop-only with no mobile fallback at all, incorrectly documented as intentional; the design source actually specifies a full mobile layout, see [[Bugs]]), `features/admin-session/useAdminSession.ts`, `platforms/admin/router/{AdminRoutes,AdminProviderShell,RequireAdminSession}.tsx`, `platforms/admin/pages/{AdminDashboardPage,AdminHostelsPage,AdminLeadsPage,AdminRevenuePage,AdminMorePage,AdminSettingsPage}.tsx`, `features/platform-admin/api/index.ts`.
- **Routes:** `/admin` (Dashboard), `/admin/hostels` (list + detail, approve/suspend/reactivate listing, assign/change subscription plan), `/admin/leads` (grid + detail drawer, status dropdown + quick Approve/Reject), `/admin/revenue` (KPIs, per-hostel subscription cards, record-payment action, CSV exports), `/admin/more`, `/admin/settings` (General / Subscription Plans / Admin Users / Notification Templates, 4 cards).
- **Depends on:** [[APIs]] (Platform Admin Console), [[Database]], [[Decisions]] ADR-030, [[Bugs]].
- **Notes (2026-07-30 addition):** the header bell (previously static, always "No notifications yet.") is now real — backed by `GET /api/platform-admin/notifications`, polled every 60s. Its data source, `composeRecentActivity()` (`lib/services/platform-admin-activity-service.ts`, new), is the *same* recent-leads/hostels/paid-invoices union the Dashboard's Recent Activity feed already computed inline — extracted into one shared function both call sites now use, so the two surfaces can't drift out of sync with each other (the `docs/business-logic/financial-consistency-investigation-report.md` compose-don't-reimplement pattern, applied here to activity feeds rather than financial reads). See [[APIs]], [[Backend]], [[Changelog]].
- **Notes:** This is a **deliberate reversal of ADR-006** — the multi-hostel SaaS billing/subscription model that was previously removed — built at the user's explicit direction after being told directly that it would reverse a documented architectural decision (see the ADR for the full exchange). `Role` gained a real `ADMIN` value (previously present only in stale migration history, never actually assignable — `schema.prisma` had only `OWNER`/`TENANT` until this feature). No public self-serve admin signup; `RequireAdminSession` mirrors `ProtectedTenantRoute`'s shape. Login/redirect wiring required two small existing-code fixes, both documented in [[Bugs]]: `LoginPage.tsx`'s `navigateForUser` sent `role === 'admin'` to the owner dashboard (ADMIN was never a real login-reachable role before), and `GET /api/auth/me`'s `is_admin` field was hardcoded `false`. A third, more significant pre-existing bug was found and fixed while building the Broadcast Notice feature: `notifications.id` had no schema default, so `NotificationService.createNotification()` had *always* failed silently everywhere it was called in this codebase (not just this new feature) — see [[Bugs]] for the fix and its implication for Food's publish-notification fan-out. Revenue's "Record subscription payment" action and Hostels' "Assign/change plan" control are real actionable UI — this is not a read-only reporting dashboard. GST is computed at 18% at CSV-export time from `platform_invoices.amount`, not stored. Verified end-to-end against the live database via a real login (not a minted token) for the bootstrapped admin account: approved/suspended/reactivated a real hostel listing, created and progressed a real lead through its status lifecycle, assigned a real subscription plan and recorded a real payment (confirmed in MRR/ARR/collected KPIs and the GST export), invited a second admin, created and toggled a notification template, updated general settings, and sent a real broadcast notification to both seeded owner accounts.

## Backend-only / automation features (no dedicated frontend page — cron or webhook driven)

- Monthly rent generation (`/api/cron/generate-rent`, `rent-generation-service.ts`)
- Daily reminders + late-fee engine (`/api/cron/rent-reminders`, `reminder-service.ts` — see [[Business-Rules]])
- Agreement lifecycle processing (`/api/cron/agreement-lifecycle`)
- Hostel/financial invariant checks (`/api/cron/hostel-invariants`, `/api/cron/migration-audit`)
- Move-out room release (`/api/cron/move-out-releases`)
- Payment reconciliation sweep (`/api/cron/reconcile-payments`)
- Admissions reservation expiry (`/api/cron/admissions`)
- Payment webhooks: PhonePe/Razorpay (`/api/webhooks/payments/razorpay`), WhatsApp Cloud API (`/api/webhooks/notifications/whatsapp`)
- Admin finance-ops dashboards (`/api/admin/finance-ops/*`, `/api/admin/finance/reconciliation/*`) — ADMIN/OWNER-only, no confirmed dedicated frontend surface found in `apps/frontend`; **Unknown** whether these are consumed by a separate internal tool or only via direct API access.

## Decommissioned features (confirmed removed, not just undocumented)

Multi-hostel SaaS billing/subscription/add-on/usage-plan system — 37 route files now 410. See [[APIs]] and [[Decisions]]. Do not build new work assuming subscription/plan/usage-quota concepts exist; they were deliberately torn out ("single-business migration").

## Feature template (for adding new entries)

```markdown
### <Feature name>
- **Status:** planned / in progress / shipped
- **Owner-facing?** yes/no · **Tenant-facing?** yes/no
- **Key files:** (backend service, frontend feature folder)
- **Depends on:** [[Business-Rules]] rule(s), other features
- **Notes:**
```

## See also
- [[Business-Rules]] for the domain rules features must respect
- [[APIs]] for the endpoints backing each feature
- [[Bugs]] for known issues per feature
- [[TODO]] for planned/unverified items surfaced during this audit
