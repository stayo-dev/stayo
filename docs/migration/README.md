# Migration docs

Home for Shri Adithya Hostel Management System → StayO rebrand/migration planning: cutover checklists, rename/rebrand tracking, data-migration notes, and anything else specific to the transition itself (as opposed to `docs/build-guide/`, which covers ordinary local setup and deployment for the app as it exists today).

Nothing has been written here yet — add docs as the migration is planned.
